# PiiAura

ERP mobile application monorepo.

## Structure

```
PiiAura_App/
  frontend/     Mobile app + shared frontend packages
  backend/      API services (to be added)
```

## Development

```bash
pnpm install
pnpm mobile
```

Set `EXPO_PUBLIC_API_URL` in your environment when the backend is ready.
